/**
 * `server-only` paketining test uchun bo'sh o'rnini bosuvchi.
 *
 * Haqiqiy paket import qilinishi bilan xato tashlaydi (u Next.js
 * build'ida server modullari client'ga tushib qolmasligini
 * qo'riqlaydi). Vitest'da esa server mantiqini ham sinash kerak,
 * shuning uchun `vitest.config.ts` da shu fayl alias qilingan.
 */
export {};
